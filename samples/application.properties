# Legacy Java application configuration with plaintext secrets and machine-bound values.

# Plaintext database credentials checked into source control.
db.url=jdbc:mysql://PROD-DB01:3306/orders
db.username=root
db.password=P@ssw0rd123

# Hardcoded internal endpoint and local file path.
payment.gateway.url=http://payments.internal.contoso.com/api/charge
log.path=/var/app/logs/order-service.log

# Machine-bound / non-portable settings.
server.host=PROD-APP01
cache.provider=in-memory
feature.legacyMode=true
